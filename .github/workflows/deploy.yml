# This workflow will build the project and deploy it to Google Cloud Platform

name: Deploy to GCP

on:
  push:
    branches: [develop, release/*, main, feature/*]

jobs:
  develop:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - uses: './.github/workflows/_build'
